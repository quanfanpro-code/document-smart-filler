# 文档智能填表系统验收记录

日期：2026-09-08。范围为本次新建的独立项目。用户已批准后续自主执行，并明确先完成程序和公开发布，真实业务资料暂不提供，真实业务质量验收保留待测。

## 已通过的程序验证

本机 Windows、Python 3.14 环境运行全部自动检查：**81 项通过，合并发布版本复验用时 16.84 秒**。测试覆盖真实文档读取、模型返回与错误处理、两路请求上限、取消和重试、金额与分录关系、字段类型、来源与跨段合并、任务保存、模板导出和真实桌面控件。

检查使用实际 Word、PDF、图片和 Excel 文件；模型异常与恢复测试使用受控本机服务或替代外部服务的测试对象。它们验证程序行为，不作为真实业务识别准确率。

- Word 正文与表格顺序、内嵌图像、页眉页脚、旧格式转换、逐页 PDF、多页图片和长文切段已检查。旧格式 Word 在本机实际转换成功，原件未改变。
- 一借多贷按三条分录输出；金额、零值、负值、长编号和前导零按约定保存。多待办挤在一行的明确异常会进入核对。
- 同页不同单据、跨页互补字段、重复片段及冲突候选分别处理。仅有相同编号和相邻页、但缺少共同来源或互补信息时，保留候选并提示核对，不直接减为一条。
- 十页合成 PDF 在处理过程中停止后恢复，最终十个不同票号各保留一行，原 PDF 校验值不变。尚未开始文件也已登记，恢复不会漏掉。
- 读取失败保留已存结果，恢复成功移除旧未完成原因；源内容改变拒绝混入旧批次；固定模板副本用于恢复，原模板不被覆盖。
- 模板实际保留列位置、常用格式、既有数据和公式；不支持的结构明确拒绝。结果生成独立版本，文件占用或导出失败不撤销已保存的提取进度。
- 密钥默认不持久保存。服务完整地址、独立主机地址和密钥会从模型请求错误及原始回答记录中隐去。

## 实际 Windows 入口

通过原项目中的“启动程序.bat”打开真实主窗口：批处理调用本机 Python 窗口程序，再进入“启动.py”和正式界面。Windows 换行问题已在真实入口复现，修复后原路径启动在约 2.17 秒内出现窗口并正常关闭。

另通过真实窗口的选择材料、保存位置、开始、停止、恢复、打开结果按钮，走完真实客户端、受控服务、保存和 Excel 导出。第一份已保存、第二份正在处理时停止；恢复只补第二份，合计三次请求，最终两个正常结果，无重复批次，源文件未改变，默认密钥未写入设置。

主界面截图由独立空演示设置生成并人工查看，包含完整中文操作区，不含私人连接信息。截图用于说明界面，真实批处理入口另有上述单独验证。

## 实际模型服务的合成材料联调

使用用户指定的模型服务，真实发送文字、图片及由 PDF 转成的图像。地址和密钥仅由本机配置及本次进程提供，不写入公开文件。下表为修复提取要求后的第二轮结果。

| 材料 | 预期结果 | 实际正常结果 | 需核对 | 请求次数 | 实测耗时 | 服务报告的输入/输出用量 |
|---|---:|---:|---:|---:|---:|---|
| 同页两张合成发票图片 | 2 行 | 2 行 | 0 | 1 | 15.45 秒 | 1418 / 541 |
| 一借两贷合成凭证 PDF | 3 行 | 3 行 | 0 | 1 | 17.91 秒 | 1846 / 626 |
| 含两期付款的合成合同 Word | 1 行 | 1 行 | 0 | 1 | 20.34 秒 | 576 / 727 |
| 两项待办的合成会议纪要 Word | 2 行 | 2 行 | 0 | 1 | 39.75 秒 | 555 / 1389 |

以上用量为服务返回的文本计量单位，四次合计 7678，不代表费用或真实业务性能承诺。未自行估算服务没有报告的统计。

按事先确定的合成材料内容，实际比对通过：两张发票的号码与三个金额；凭证三个科目、字号及借贷合计；合同总额及两期付款安排；两项会议待办与各自责任人、截止日期。共八行业务结果均成功导出。

第一轮曾出现凭证另一侧空白未说明原因、会议两项待办挤在一行。已修正提示要求，并补充明确异常核对测试；没有通过放松金额或缺失检查把异常改成正常。

## 原件保护与独立审查

两个老系统只读参考。对最初保存的 74 个源代码文件逐个重新计算校验值，变化数量为 0。新项目不动态引用老项目配置、缓存或客户资料。

独立审查提出的输入清单遗漏、旧提示残留、读取错误覆盖旧结果、结果目录误排除、相邻页同号票据误合并等问题均已复现并修复。修复后完成全套复验。没有为本次需求增加插件框架或外部识别服务。

## 开源及发布检查

许可证为完整 GNU AGPL 第三版，项目使用仅第三版。标准原文来自 [GNU](https://www.gnu.org/licenses/agpl-3.0.txt)，并与 [SPDX 官方文本](https://raw.githubusercontent.com/spdx/license-list-data/main/text/AGPL-3.0-only.txt)核对条款。引用组件仍遵守各自许可证。

公开候选包含源代码、自动检查、需求设计与说明、无私人信息的截图和依赖清单。本机设置、模型回答、批次数据库、结果文件和聊天工作文件均排除。推送前对实际服务器完整地址、独立 IPv4 和密钥进行精确扫描，只输出是否命中，不显示匹配内容。

已发布到 [GitHub公开仓库](https://github.com/quanfanpro-code/document-smart-filler)。远端主分支提交与本机一致，README、完整许可证及截图逐个比对一致，GitHub已识别为AGPL-3.0。

[Windows自动检查](https://github.com/quanfanpro-code/document-smart-filler/actions/runs/34245602486)运行成功：80项通过，1项因为云端没有Word而跳过，用时27.33秒。该项已在本机实际通过。初次配置单独上传期间的过渡检查不作为产品验收证据。

实际公开文件共34个；公开文件及全部提交历史的私人完整地址、独立IPv4和密钥精确扫描命中均为0。截图另已人工查看确认不包含连接信息。

## 明确保留待测

- 40 份真实业务文件、每类 10 份，以及人工核对标准答案。
- 四类分别的关键字段准确率、整行正确率、漏项与重复数量、正常样本自动完成比例。
- 真实模糊扫描件、复杂附件、跨段语义边界等实际业务识别质量。
- 20 张清晰单据、十页真实扫描 PDF、超长合同的规模性能；服务实际上下文、图片数量与尺寸上限。

以上按用户明确决定延期，不宣称已达质量门槛。当前合成材料和自动检查通过，只说明已验证的程序过程与示例结果。
